[package]
name = "buddy-cli"
version = "0.5.0"
edition = "2024"

[dependencies]
buddy-core = { path = "../buddy-core" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"
toml = "0.8"
sha2 = "0.10"
hex = "0.4"
